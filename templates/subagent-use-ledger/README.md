# Subagent-use ledger state

This directory is reserved for later explicit ledger scans. Reports must contain metadata and structured fields only; never store prompts, objectives, checkpoints, credentials, or result excerpts here.
