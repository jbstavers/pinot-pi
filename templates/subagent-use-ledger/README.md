# Subagent-use ledger state

This directory is reserved for a later explicit Pinot ledger scan. Unit 5 owns scanner integration and report generation; Unit 4 does not implement a scanner or launcher.

The ledger is an aggregate period evidence source. A report path and coverage end may be referenced by an implementation record, but no period is claimed as uniquely attributable to that implementation. Do not copy prompts, objectives, checkpoint bodies, credentials, sessions, transcripts, caches, or generated reports into implementation history.
