# Implementation record: `<project> / <implementation>`

> This append-only record contains semantic implementation provenance.

- **Date:** `YYYY-MM-DD`
- **Outcome:**
- **Project path or stable project identifier:**
- **Source commits / range:**

## Sessions and provenance

- **Root Pi session:** name — ; ID — ; path —
- **Durable children:**
  - name — ; session — ; checkpoint reference — ; final host state —
  - `none`
- **Background agents:**
  - role — ; bounded purpose — ; disposition —
  - `none`

## Review and verification

- **Review findings / dispositions:**
- **Verification:**
- **Deviations:**
- **Remaining work:**

## Janitor maintenance

- **Closeout mode and date:**
- **Documentation changes:**
- **Cleanup classifications:** delete — ; retain — ; unresolved —
- **Open questions:**

## Historical snapshots

These are exact historical snapshots, not current governing documents.

- **Specification:** `specification.md` or `none`
- **Plans:** list every copied snapshot; `plan.md` is primary; each additional supplied plan uses `plan-<slug>.md`; `none` only when no plan exists
  - `plan.md` — primary — source path —
  - `plan-<slug>.md` — additional label — source path —
- **Source handoff path(s):** exact tracked or ignored path(s) —

Only final, explicitly supplied documents may be copied when safe to track. Never concatenate plans. Do not copy transcripts, checkpoint bodies, credentials, authentication state, sessions, caches, generated diagnostic output, or uncertain inputs.

## Debrief lookup

- **Project / commit pointers:**
- **Safe snapshot pointers:**
- **Review / verification pointers:**
- **Remaining-work pointers:**
