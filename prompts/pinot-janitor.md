---
description: Run explicit closeout, documentation, or conservative project cleanup
argument-hint: "<closeout|docs|sweep> [scope]"
---
Run Pinot Janitor in `${1}` mode for the optional scope `${@:2}`. The supplied request is: $@. If no mode is supplied, ask the user to choose `closeout`, `docs`, or `sweep`.

Load the package-owned `pinot-janitor` skill and its relative references before inspection. Start the fresh specialist through `pinot_native_herdr_implementer` with the start-only `janitor` profile and explicit skill loading; recover and verify its immutable profile metadata on every later lifecycle action. All closeout actions require a functioning Herdr environment and Pi integration; verify the prerequisite before writing. If Herdr is unavailable, stop with a clear setup message. Never fall back to root editing or an ephemeral cleanup worker.

`closeout` requires an exact implementation handoff and, for history, the exact configured history root supplied by the coordinator. `docs` reviews durable project documentation against current behavior. `sweep` makes a bounded inventory. Do not reconstruct work from transcripts.

Keep one writer total. The Janitor specialist is the sole writer while active and must not commit or delegate. It may edit assigned project documentation and high-confidence project-local ephemeral artifacts only, plus the explicitly supplied history-root exception. It must not change product behavior or infer authority over another repository. Never delete tracked source/tests, active work, uncertain human input, credentials, authentication state, application data, Pi sessions, checkpoints, caches, backups, generated reports, or anything outside scope.

For every candidate classify `delete`, `retain`, or `unresolved`; uncertainty is retained. Closeout preserves semantic provenance, review and verification, deviations, remaining work, append-only index maintenance, and safe exact spec/plan snapshots without overwriting or suffixing collisions. Aggregate ledger references are period evidence only and must not claim per-implementation attribution. The Janitor skill must not copy transcripts, checkpoint bodies, credentials, sessions, caches, generated reports, or uncertain inputs.

Before delivery, inspect the diff and deletions, run only named checks, write a concise checkpoint, and report changed/deleted paths, classifications, documentation decisions, validation, open questions, and worktree state.
